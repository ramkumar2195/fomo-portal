"use client";

import { SectionCard } from "@/components/common/section-card";

export default function ClassSchedulePage() {
  return (
    <SectionCard title="Class Schedule" subtitle="Placeholder">
      <p className="rounded-lg bg-slate-100 px-3 py-2 text-sm text-slate-600">
        No class-schedule endpoint exists in the current backend. This page remains a placeholder until schedule APIs
        are available.
      </p>
    </SectionCard>
  );
}
